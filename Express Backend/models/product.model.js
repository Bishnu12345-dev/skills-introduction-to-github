import mongoose from "mongoose";

// 👉 Define product schema here
// Example fields you might include:
// - name: string, required
// - price: number, required
// - description: string, optional
// - category: string
// - stock: number
// - isAvailable: boolean (default true)
// - ratings: array of numbers or reference to review model
// - imageUrl: string for product image

// 👉 Use timestamps to track when product was added or updated
// const productSchema = new mongoose.Schema({ ... }, { timestamps: true });

// 👉 Export Product model
// export default mongoose.model("Product", productSchema);
